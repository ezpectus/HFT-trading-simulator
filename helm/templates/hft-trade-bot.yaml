{{- /*
  HFT Trade Bot is deployed as a sidecar container in the ai-signal-bot pod
  to share /dev/shm for SHM IPC. See ai-signal-bot.yaml for the Deployment.
  This file is intentionally empty — hftTradeBot.enabled is checked in
  ai-signal-bot.yaml to conditionally add the sidecar container.
*/ -}}
