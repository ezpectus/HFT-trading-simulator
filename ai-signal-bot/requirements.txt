# AI Signal Bot — Requirements
pyyaml==6.0.2
websockets==13.1
matplotlib==3.9.3
numpy==2.1.3

# Optional dependencies (used with try/except ImportError — not required for core bot)
aiohttp==3.14.3
prometheus-client==0.21.1

# Test dependencies (pip install -r requirements.txt -r requirements-dev.txt)
# pytest==8.3.4
# pytest-asyncio==0.24.0
