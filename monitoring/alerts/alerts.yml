# Prometheus Alert Rules
#
# Defines alert rules for trading system metrics.
# All metric names match those exposed by:
#   - exchange_simulator/ws_prometheus.py (exchange_* metrics)
#   - ai-signal-bot/src/communication/metrics_server.py (ai_signal_bot_* metrics)

groups:
  - name: error_rate_alerts
    interval: 30s
    rules:
      - alert: HighBotErrorRate
        expr: rate(ai_signal_bot_errors_total[5m]) > 0.1
        for: 5m
        labels:
          severity: warning
          component: ai-signal-bot
        annotations:
          summary: "High bot error rate"
          description: "Error rate is {{ $value }} errors/sec"

      - alert: CriticalBotErrorRate
        expr: rate(ai_signal_bot_errors_total[5m]) > 1.0
        for: 2m
        labels:
          severity: critical
          component: ai-signal-bot
        annotations:
          summary: "Critical bot error rate"
          description: "Error rate is {{ $value }} errors/sec"

  - name: trading_alerts
    interval: 60s
    rules:
      - alert: HighDrawdown
        expr: ai_signal_bot_drawdown > 0.08
        for: 5m
        labels:
          severity: warning
          component: ai-signal-bot
        annotations:
          summary: "High drawdown"
          description: "Drawdown is {{ $value * 100 }}% (threshold 8%)"

      - alert: CriticalDrawdown
        expr: ai_signal_bot_drawdown > 0.15
        for: 2m
        labels:
          severity: critical
          component: ai-signal-bot
        annotations:
          summary: "Critical drawdown"
          description: "Drawdown is {{ $value * 100 }}% (threshold 15%)"

      - alert: LowWinRate
        expr: ai_signal_bot_win_rate < 0.4
        for: 30m
        labels:
          severity: warning
          component: ai-signal-bot
        annotations:
          summary: "Low win rate"
          description: "Win rate is {{ $value * 100 }}%"

      - alert: NegativePnL
        expr: ai_signal_bot_pnl_total < 0
        for: 10m
        labels:
          severity: warning
          component: ai-signal-bot
        annotations:
          summary: "Negative PnL"
          description: "Total PnL is {{ $value }}"

  - name: exchange_simulator_alerts
    interval: 30s
    rules:
      - alert: HighOrderRejectionRate
        expr: exchange_orders_rejected_total / exchange_orders_submitted_total > 0.1
        for: 5m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "High order rejection rate"
          description: "Rejection rate is {{ $value * 100 }}% for {{ $labels.exchange }}"

      - alert: NoConnectedClients
        expr: exchange_connected_clients == 0
        for: 2m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "No WebSocket clients connected"
          description: "No clients connected to exchange simulator for 2+ minutes"

      - alert: TradingStopped
        expr: exchange_trading_active == 0
        for: 1m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "Trading is STOPPED"
          description: "Trading has been stopped. All order submissions are being rejected."

      - alert: HighDisconnectionRate
        expr: rate(exchange_ws_disconnections_total[5m]) > 0.5
        for: 5m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "High WebSocket disconnection rate"
          description: "Clients disconnecting more than 0.5/s over 5 minutes"

  - name: hft_alerts
    interval: 30s
    rules:
      - alert: LowFillRate
        expr: exchange_orders_filled_total / exchange_orders_submitted_total < 0.8
        for: 10m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "Low order fill rate"
          description: "Fill rate is {{ $value * 100 }}% for {{ $labels.exchange }} (threshold 80%)"

      - alert: CircuitBreakerOpen
        expr: ai_signal_bot_circuit_breaker_state == 1
        for: 1m
        labels:
          severity: critical
          component: ai-signal-bot
        annotations:
          summary: "Circuit breaker is OPEN"
          description: "Trading circuit breaker tripped — all signals blocked"

      - alert: NoSignalsSent
        expr: rate(ai_signal_bot_signals_sent_total[10m]) == 0
        for: 15m
        labels:
          severity: warning
          component: ai-signal-bot
        annotations:
          summary: "No signals sent in 15 minutes"
          description: "Signal generation may be stalled"

      - alert: EquityDrop
        expr: exchange_equity < exchange_balance * 0.95
        for: 5m
        labels:
          severity: warning
          component: exchange-simulator
        annotations:
          summary: "Equity below 95% of balance"
          description: "Unrealized losses exceeding 5% for {{ $labels.exchange }}"

      - alert: CandleGenerationStalled
        expr: rate(exchange_candle_count[5m]) == 0
        for: 5m
        labels:
          severity: critical
          component: exchange-simulator
        annotations:
          summary: "Candle generation stalled"
          description: "No new candles generated in 5 minutes — market data feed may be down"
