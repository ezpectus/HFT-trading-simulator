# Encrypted secrets template (SOPS)
# ================================
# This file is a TEMPLATE. To create encrypted version:
#   sops deploy/k8s/secrets.enc.yaml
#
# After encryption, the values will be base64+age encrypted.
# Never commit unencrypted secrets to git!

apiVersion: v1
kind: Secret
metadata:
  name: hft-db-secret
  namespace: hft-trading
type: Opaque
stringData:
  password: "CHANGE_ME_TIMESCALEDB_PASSWORD"

---
apiVersion: v1
kind: Secret
metadata:
  name: hft-exchange-secrets
  namespace: hft-trading
type: Opaque
stringData:
  BINANCE_API_KEY: "CHANGE_ME"
  BINANCE_API_SECRET: "CHANGE_ME"
  BINANCE_TESTNET_API_KEY: "CHANGE_ME"
  BINANCE_TESTNET_API_SECRET: "CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: hft-notify-secrets
  namespace: hft-trading
type: Opaque
stringData:
  TELEGRAM_BOT_TOKEN: "CHANGE_ME"
  TELEGRAM_CHAT_ID: "CHANGE_ME"
  DISCORD_BOT_TOKEN: "CHANGE_ME"
  DISCORD_CHANNEL_ID: "CHANGE_ME"

---
apiVersion: v1
kind: Secret
metadata:
  name: hft-llm-secrets
  namespace: hft-trading
type: Opaque
stringData:
  OPENAI_API_KEY: "CHANGE_ME"
