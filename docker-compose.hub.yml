# Docker Hub — Pre-built Images
# ====================================
# Uses pre-built images from Docker Hub instead of building from source.
# No compilation required — just `docker-compose -f docker-compose.hub.yml up`.
#
# Images:
#   ezpectus/hft-exchange-simulator:v2.0.0
#   ezpectus/hft-ai-signal-bot:v2.0.0
#   ezpectus/hft-trade-bot:v2.0.0
#   ezpectus/hft-web-ui:v2.0.0
#
# Usage:
#   docker-compose -f docker-compose.hub.yml up
#   docker-compose -f docker-compose.hub.yml up -d        # detached
#   docker-compose -f docker-compose.hub.yml logs -f      # follow logs
#   docker-compose -f docker-compose.hub.yml down          # stop all
#
# Web UI: http://localhost:3000

services:
  exchange-simulator:
    image: ezpectus/hft-exchange-simulator:v2.0.0
    container_name: exchange-sim
    ports:
      - "8765:8765"
      - "8775:8775"
    volumes:
      - ./exchange_simulator/config.yaml:/app/config.yaml:ro
      - sim-data:/app/data
      - sim-logs:/app/logs
    command: python -m exchange_simulator --no-visualizer
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8775/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - trading-net

  ai-signal-bot:
    image: ezpectus/hft-ai-signal-bot:v2.0.0
    container_name: ai-signal-bot
    depends_on:
      exchange-simulator:
        condition: service_healthy
    ports:
      - "8766:8766"
      - "9090:9090"
    volumes:
      - ./ai-signal-bot/config/settings.yaml:/app/config/settings.yaml:ro
      - ai-data:/app/data
      - ai-logs:/app/logs
    command: python run.py --metrics
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9090/health', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - trading-net

  hft-trade-bot:
    image: ezpectus/hft-trade-bot:v2.0.0
    container_name: hft-trade-bot
    depends_on:
      exchange-simulator:
        condition: service_healthy
      ai-signal-bot:
        condition: service_healthy
    volumes:
      - ./hft-trade-bot/config:/app/config:ro
      - hft-logs:/app/logs
    ports:
      - "9091:9091"
    command: ./build/hft_trade_bot config/config.yaml
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:9091/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - trading-net

  web-ui:
    image: ezpectus/hft-web-ui:v2.0.0
    container_name: web-ui
    depends_on:
      exchange-simulator:
        condition: service_healthy
      ai-signal-bot:
        condition: service_healthy
    ports:
      - "3000:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
    networks:
      - trading-net

volumes:
  sim-data:
  sim-logs:
  ai-data:
  ai-logs:
  hft-logs:

networks:
  trading-net:
    driver: bridge
